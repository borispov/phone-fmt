'use strict'

var fmt = require('./phoneFormat').fmt

/**
 * Formats Israeli mobile number to a standard international convention
 * @param {number} number
 * @return {string}
 */

module.exports = fmt